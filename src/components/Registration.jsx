const Registration = () => {
    return (
        <div className="hidden lg:flex justify-end gap-7">
            <button className="register-btn underline">Login</button>
            <button className="register-btn border-2 border-accent px-8 py-2 rounded-full">Sign Up</button>
        </div>
    );
}

export default Registration;
